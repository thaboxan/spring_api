# Since you already have a database deployed, just deploy the web service
services:
  # Spring Boot Web Service  
  - type: web
    name: spring-api-backend
    runtime: java
    plan: free
    buildCommand: "chmod +x build.sh && ./build.sh"
    startCommand: "chmod +x start.sh && ./start.sh"
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_URL
        value: "postgresql://todolist_rddd_user:LwPa8KxAq4cjekhLRvxwATRI0Zom0sfO@dpg-d3ahrj95pdvs73csvie0-a.oregon-postgres.render.com/todolist_rddd"
